# Weather-Based-Sensor-Calibration-Suggestion-System
A live, self-adjusting calibration layer for Perimeter Intrusion Detection Systems (PIDS) that reads real weather, reasons about it in plain language, and tells operators — in real time — exactly how sensitive their sensors should be right now.
